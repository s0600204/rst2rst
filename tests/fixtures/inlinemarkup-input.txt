Inline markup *ought* always be serialized back to their
:emphasis:`minimal` form. It is **important** that inline markup be
:strong:`normalized`. The final ``markup`` role is
:literal:`literal`.

*Nesting inlines is :strong:`not` valid*.

************************************
A *title* may have :strong:`inlines`
************************************

Escapes should *\*remain\**.
