Literals *ought* always be serialized back to their *literal* form. It is
**important** that literals be **normalized**. The final ``literal`` role is
``literal``.

*Nesting inlines is :strong:`not` valid*.


******************************
A *title* may have **inlines**
******************************

Escapes should *\*remain\**.
