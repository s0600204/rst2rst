Inline markup *ought* always be serialized back to their *minimal* form. It is
**important** that inline markup be **normalized**. The final ``markup`` role
is ``literal``.

*Nesting inlines is :strong:`not` valid*.


******************************
A *title* may have **inlines**
******************************

Escapes should *\*remain\**.
